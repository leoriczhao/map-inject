# tools/config.example.ps1 — Local configuration template
# Copy this to config.local.ps1 and edit for your environment.
# config.local.ps1 is gitignored — won't be committed.

# War3 installation directory
$War3Dir = "D:\Warcraft III Frozen Throne 1.27a publish"

# War3 log file path (for deploy.ps1 test result polling)
# $LogFile = "$War3Dir\japi-debug.log"

# Custom DLL path (if not using default build output)
# $DllPath = "path\to\yd_japi.dll"

# Custom base map path
# $BaseMap = "path\to\base.w3m"
